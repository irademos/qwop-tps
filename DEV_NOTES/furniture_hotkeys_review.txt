Review of added file: features/furnitureRotationHotkeys.js

Summary:
- No other files need to be added. app.js already lazy-loads and initializes the hotkeys
  when the furniture placement preview is created (it imports
  './features/furnitureRotationHotkeys.js' and calls initFurnitureRotationHotkeys
  with the snapController and toasts).

How to verify locally:
1) Start a simple static server from the project root:
   python3 -m http.server 8000
2) Open http://localhost:8000/ in a browser.
3) Activate the furniture placement preview (press P if the in-game controls are present),
   then use '[' and ']' to rotate and 'K' to cycle snap modes.

If you'd like, I can:
- Wire the hotkeys to a different controller or default to global window.furnitureRotationSnapping.
- Change default snap angles or the "free" step size.

No edits to other files required at this time.
