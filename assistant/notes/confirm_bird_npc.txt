No additional files are required.
- Reason: app.js already dynamically imports ./features/birdNPC.js in initBirdNPC() and activates it, so adding features/birdNPC.js is sufficient.

How to verify:
- Run a simple local server from the project root (e.g. python -m http.server 8000) and open index.html.
- The bird NPC should be created and orbit the player; chirp SFX may be blocked by browser autoplay/security policies.

If you want next:
- I can add a lightweight debug HUD entry (non-interactive) to confirm the bird is active.
- Or implement the next ai_meta task (pick one) and wire it into app.js exactly once.

Committed changes should be limited to small modules and lazy-loaded controllers per UX guardrails.
